# 桌面助手：双击启动

在项目根目录双击 **桌面助手启动.cmd**。短暂启动窗口关闭后，会出现“作业管理系统 · 桌面助手”，自动检查配置、准备页面并启动系统。首次或源码更新后会构建前端，稍等即可；就绪后默认打开浏览器。

常用按钮：一键启动、打开系统、安全停止、查看日志、使用说明、数据库迁移指南。重复双击不会再创建一套服务，会提醒切换到已有助手。可把此文件“发送到桌面 → 创建快捷方式”，不要只复制启动文件离开项目。

## 首次环境准备（当前电脑已准备）

另一台电脑需 Python 3.10+（含 Tcl/Tk）、Node.js/npm、MySQL，以及项目依赖。由编程助手在项目根目录运行：

```powershell
python -m pip install -r backend/requirements-desktop.txt
npm.cmd --prefix frontend ci
```

保留 `backend/.env` 的本地配置，不向 Git 提交真实密码。数据库搬家必须先阅读 [数据库迁移指南](数据库/数据库迁移指南.md)。助手不会自动建库、迁移、重置账号、联网安装依赖或调整系统权限。生产模式要求两项签名密钥足够强，缺失会拒绝启动。

## 运行方式

- 默认入口：`http://127.0.0.1:5000/`。同一地址提供打包页面和 API，不再依赖 3000 端口的 Vite 开发服务。
- 使用 Waitress，关闭 Flask debug、自动建表及公开注册；沿用已有数据库与账号。仅监听本机，不设置开机自启、Windows 服务、防火墙规则或公网访问。
- 每次先检查数据库连通、迁移头及模型字段。缺依赖、数据库未启动、迁移不匹配、端口占用和构建失败会显示提示，不将失败冒充启动成功。
- 页面源码变化时自动构建（需 Node 及前端依赖），无变化复用产物。构建只处理本地源码，不自动安装包；默认强制同源 `/api`。
- Redis/Qdrant 保持现有配置，由其原服务方式管理。助手不自动启动外部 AI worker，避免消费已有队列或产生模型费用；本地教学和知识检索可以独立使用，外部 AI 的可用性不是此助手的就绪保证。
- 此入口更适合本机日常使用，但不等于已通过全部业务安全/并发验收。公网部署仍需 HTTPS、权限专项验证、备份和正式运维方案。

技术依据：[Flask 官方 Waitress 部署说明](https://flask.palletsprojects.com/en/stable/deploying/waitress/)；Waitress 可在 Windows 运行，这不是将 Flask 开发服务器包装成“生产服务”。

## 停止和故障处理

1. 日常停止请用“安全停止”。关闭浏览器不会停服务；关闭助手会询问是否停止并退出。
2. 停止时新进入应用的请求返回 503，正在执行的请求最多等待 30 秒；未完成则取消本次停止并提示，不强杀。请在没有师生操作时停止；这不替代弱网幂等和并发提交修复。
3. 助手意外退出时，服务检测父进程管道关闭，等待执行中的请求完成后退出。断电/强制结束系统仍可能中断请求，不能视为事务或备份保证。
4. 端口 5000 被旧 `start_all.py` 等占用：在原窗口停止旧服务后重试。助手不接管未知服务、不按端口杀进程。不要同时使用新旧启动器。
5. 日志在 `数据库/local/desktop-assistant/`；`launcher.log` 记录窗口启动问题，`run-*.log` 记录各次构建和服务日志。该目录已被 Git 忽略；不要公开上传日志，可能含业务异常信息。日志不会自动删除，定期按需归档。
6. 如果没有窗口：查看 `launcher.log`，确认 Python 的 Tkinter 可用。若系统禁用命令脚本，可由编程助手运行 `python -m desktop_assistant.app`。移到其他电脑后重新创建快捷方式。

开发人员仍可使用原 `start_all.py`。桌面助手的实现集中在 `desktop_assistant/`，不替换原数据库备份和迁移工具。

本机启停结果与未完成的视觉检查见 [桌面助手验证报告](desktop_assistant/验证报告.md)。
