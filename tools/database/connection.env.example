# 复制为 connection.local.env 后，仅在本地填写。不要提交真实连接信息。
# 迁移工具 --env-file 放在 check/backup/restore/verify 子命令之前。
# 此处数据库名可以是尚未创建的新库；restore 会连接服务器而不是旧数据库。
DATABASE_URL=mysql+pymysql://migration_operator:REPLACE_WITH_URL_ENCODED_PASSWORD@127.0.0.1:3306/homework?charset=utf8mb4
