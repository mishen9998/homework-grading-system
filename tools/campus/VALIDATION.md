# t1 开发自测记录（2026-09-17）

strategy=multi-feature。本记录是本任务开发自测，不替代独立检查、校园最终验收或原商业验收。

真实运行 full `f3843eb16ce8`，入口 `http://127.0.0.1:18104`；small `376e0a3d06e4` 已停止并保留卷。完整公开证据由主工作区 `pipeline-workspace/tasks/t1/evidence/full`、`small` 保存；本工作树 `.campus-runs/<run_id>` 为运行副本，均无明文凭据。运行及复核入口见 README。

| 自测 | 实际结果 |
| --- | --- |
| 真实依赖 | Python 3.11.14、MySQL 8.0.39、Redis 7.2.5、Qdrant 1.15.4，Node22.19.0；依赖完整锁定 |
| full SQL | 9500学生、500教师、6管理员、5教学机构+1平台机构；250班、500课、19000成员关系、2000作业、20000题、51300提交、513000答案 |
| 四阶段提交 | 19000 / 19000 / 9500 / 3800；7600去重学生可供后续截止场景 |
| full附件 | 5130份（10%）；33564672字节；每文件大小及SHA-256通过 |
| small SQL | 190学生、10教师；1026提交、10260答案、103附件；表及关系哈希通过 |
| 重跑与错档 | 两档同档seed只验证；另一个profile明确拒绝；之后表哈希与附件哈希保持原值 |
| 真实HTTP | 每档1学生+1教师，各登录及课程读取一次，4次200；不计为万人参与 |
| 目标门禁 | 7项新纯参数测试通过；拒绝非回环/原3306及6379/缺run_id/带凭据或路径目标，无连接探测 |
| full最后健康 | MySQL、Redis、Qdrant、后端、依赖worker及回环网关全部healthy；HTTP ready成功 |
| Redis范围 | 实际键1个，全部在本轮命名空间 |
| SQL空间 | full表分配85819392字节；small3604480字节（快照，非磁盘峰值） |
| 外部AI | 配置关闭、无API key、内部网络及应用连接门禁；未作公网探测 |

full内容指纹：`65b227a6c5c885b9e4ebcc5ef2b712aa68eb625c97d00b4348ce08d925a33597`。附件manifest指纹：`7ce7cd17ed54b250fa7819af58ade064661b7b83074ba1e31c145f0114534b45`。这些指纹含本轮附件命名；其他run需按自身run_id重新生成预期。

曾遇到并已修复：Docker忽略规则误排除Python模型源码；原基础依赖缺少课表导入必需的pandas；Docker Desktop无法发布纯internal网络端口（增加只绑定回环的固定反向代理）；健康探针不必要导入Flask导致共享资源紧张时超时（改为创建应用时按需导入）。最后健康快照已通过，之前失败快照另保留，未删除或改写为成功。full存活环境拷入了最终健康导入修正和环境采集脚本；其原始构建镜像ID仍如实保存在identity.json。最终重建镜像使用工作树路径派生的专属标签，避免下游并行工作树覆盖同一构建标签。

未执行：旧被阻止检查、原商业完整套件、10000账号参与、50/500/1000VU容量、截止协议/故障场景、浏览器、知识索引/语义质量、备份恢复、历史迁移。心跳worker仅证明依赖可用，Qdrant只启动并预留命名空间；这些不冒充t2–t6或旧完整AI任务完成。
