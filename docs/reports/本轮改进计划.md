# 本轮改进（单助手顺序执行）

范围：当前根目录的 Flask/Vue 项目；不合并尚未验收的 campus/saas 分支，不恢复旧流水线，不把本轮自检标为独立验收。

1. 确认本地 MySQL 连接；提供不回显密码的检查、完整备份、空目标恢复和校验工具。
2. 根目录“数据库”保存迁移指南、脱敏配置示例、私有备份及校验清单；不导出明文账号密码表。
3. 修复测试引用、运行配置和前端请求缓存问题，验证后端回归及前端构建。
4. 接通持久化知识分块；检索请求只读，不现场批量计算文档向量；全文候选按相关度排序，过滤失效向量和过期缓存。
5. 使用独立数据库演练恢复/升级；用合成资料验证检索，实际业务库不灌入演示资料。

验收维度：功能、错误处理、数据完整性、回归、迁移可复现性。千人并发、商业上线、独立第三方验收均不在本轮范围。

初始检查：本地 MySQL 8.0.45 可连接；业务库 homework 有 359 个账号、0 条知识，无 Alembic 版本表，缺少知识分块表。原有 15 项测试：12 通过、3 项 mock 引用失效。修改前先备份，数据库升级先在副本执行。
