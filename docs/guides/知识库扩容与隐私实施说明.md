# 知识库扩容与隐私实施说明

## 当前已经落地

项目现在支持两种运行方式。开发环境只启动 Flask、MySQL 和前端即可，检索使用本机模型、数据库向量和进程内缓存。生产环境配置 Redis 与 Qdrant 后，多个后端进程共享查询缓存、索引版本和账号限流，并通过 Qdrant 的 HNSW 索引取得语义候选条目。

检索顺序如下：

1. 根据当前登录角色固定学生库或教师库。
2. Redis 查询角色库、模型版本、问题相同的缓存结果。
3. MySQL 8 使用中文 ngram 全文索引选取关键词候选；Qdrant 在相同角色库和相同向量模型范围内选取语义候选。
4. 后端重新从 MySQL 读取候选并再次校验 `library` 与 `approved` 状态。
5. 对候选执行关键词与语义混合排序，最多返回 5 条。
6. 管理员审核或重建索引时递增知识库版本，旧缓存自然失效。

Qdrant 只保存向量、知识条目 ID、角色库和向量模型摘要，正文仍以 MySQL 为准。Redis 和 Qdrant 发生连接错误时会熔断 15 秒并退回本机缓存与 MySQL 检索，用户请求不会持续等待外部服务超时。

## 在现有电脑上运行

`backend/.env` 中不配置 `REDIS_URL` 和 `QDRANT_URL` 即可维持当前启动方式。首次更新数据库执行：

```powershell
cd backend
python migrate_add_knowledge_scale_indexes.py
```

新增索引覆盖常用的“角色库 + 审核状态 + 时间或 ID”查询，并在 MySQL 8 上创建标题和正文的中文 ngram 全文索引。开发环境兜底候选数默认从无上限增长改为最多 1,500 条，可通过 `KNOWLEDGE_FALLBACK_CANDIDATES` 调整。

本机已经安装 Redis 服务时，可以在 `backend/.env` 配置 `REDIS_URL=redis://127.0.0.1:6379/0`。Qdrant 使用独立的本机编排启动：

```powershell
docker compose --env-file deploy.env -f compose.local-infra.yml up -d
```

该编排只将 Qdrant 绑定到 `127.0.0.1:6333`，不会启动或替换当前 MySQL。停止服务可运行 `docker compose --env-file deploy.env -f compose.local-infra.yml stop`；普通停止不会删除向量数据卷。

## 校内部署 Redis 与 Qdrant

复制 `deploy.env.example` 为 `deploy.env`，填写 MySQL、Redis、Qdrant、Flask 和 JWT 的不同随机密钥。Redis 密码建议使用只包含字母和数字的长随机值，避免放入连接地址时需要 URL 编码。

```powershell
docker compose --env-file deploy.env -f compose.production.yml up -d mysql redis qdrant
docker compose --env-file deploy.env -f compose.production.yml build backend
docker compose --env-file deploy.env -f compose.production.yml run --rm backend python -m flask --app run db upgrade
docker compose --env-file deploy.env -f compose.production.yml up -d backend frontend
docker compose --env-file deploy.env -f compose.production.yml exec backend python rebuild_knowledge_embeddings.py --force
```

最后一条命令把已有的已审核向量同步到 Qdrant。以后单篇资料在管理员通过时自动更新 MySQL 和 Qdrant，不调用 DeepSeek。Redis 和 Qdrant 在生产编排中没有映射到宿主机公网端口，只能由同一 Docker 网络中的后端访问。

## 人数增长的配置

默认生产后端为 2 个 Gunicorn 进程、每个进程 4 个线程。先通过压力测试测量峰值并发，再调整进程数；每增加一个进程都会额外加载一份本地 Embedding 模型。`DB_POOL_SIZE` 是每个进程的常驻连接数，`DB_MAX_OVERFLOW` 是短时额外连接数，所有进程的连接总数不能超过 MySQL 限制。

常用配置：

```env
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=15
KNOWLEDGE_CACHE_TTL=120
KNOWLEDGE_QUERY_RATE_LIMIT=60
KNOWLEDGE_AGENT_RATE_LIMIT=60
DEEPSEEK_QUERY_RATE_LIMIT=10
KNOWLEDGE_LEXICAL_CANDIDATES=300
KNOWLEDGE_VECTOR_CANDIDATES=80
KNOWLEDGE_FALLBACK_CANDIDATES=1500
```

注册人数本身不会造成持续压力，峰值登录、同时查询和同时交作业才决定实例数。上线前应分别压测登录、课程列表、提交作业、本地知识查询和 DeepSeek 查询，并观察 95% 响应时间、数据库连接等待、Redis 命中率和 Qdrant 查询时间。

## 隐私控制

本地查询不会把资料发送到外部。只有用户主动选择 DeepSeek 深入解答或使用原教师生成式助手时才调用外部模型。调用前系统会脱敏常见格式的身份证号、手机号、邮箱和银行卡号，管理员审核页面也会显示检测提示。

生产上线前还需要完成以下校级策略配置：

- 把知识可见范围继续细分为全校、学院、课程、班级和指定人员，并在 MySQL 与 Qdrant 查询阶段同时过滤。
- 对外部 AI 增加“允许外发”字段，敏感级资料强制只使用本地模型。
- 记录登录、审核、附件下载、外部 AI 调用和智能体写操作；审计日志只保存问题摘要哈希和来源 ID，不保存完整提问。
- 附件改用校内对象存储并开启静态加密、病毒扫描、短时下载地址与备份清除流程。
- 规定资料保留时间、作者撤回流程和删除后 MySQL、Qdrant、缓存及备份的同步清理流程。

当前自动脱敏属于防误传措施，不能替代学校的数据分级和审批制度。
