# 师生知识库

学生和教师登录后，从底部「知识库 · AI」进入各自知识库。管理员从「知识库审批」进入，可切换学生库和教师库。

- 学生库分类：校规校训、处罚条例、学院信息、楼层导览、线上课程、学长学姐分享、其他。
- 教师库分类：教学资料、学校信息、经验分享、其他。
- 两个库初始没有资料，不预填学校规定或处罚信息。

## 录入与审批

点击「上传知识」，填写标题、分类、正文，可附来源或课程链接，以及一个不超过 5MB 的附件。支持 PDF、DOCX、TXT、MD、PPTX、XLSX、PNG、JPG、JPEG。

所有新提交均为待审核，管理员提交也不例外。提交者可在「我的提交」查看状态和审核意见。管理员可阅读正文、下载附件，再通过或驳回，驳回必须填写原因。只有已通过的内容才会出现在公开书架和 AI 检索结果中。

师生只能读写各自角色的知识库。未通过审核的正文和附件，仅提交者与管理员可访问。附件经鉴权接口下载，不放在公开文件目录。

## AI 查阅

在知识库右侧（手机上为下方）向知识助手提问。默认只查询本地数据库，检索本角色已通过审核的标题和正文，直接返回相关原文摘录及来源，不调用 DeepSeek、不消耗模型 Token。原文摘录不是模型总结，用户可点击来源查看完整资料。

需要综合、解释资料时，点击该条查询结果下的「AI 深入解答（调用 DeepSeek）」，才将该问题及重新检索的已审核资料发送给 DeepSeek。没有相关资料时不调用模型；服务不可用时回退到本地原文摘录。界面区分「本地知识库查询」与「DeepSeek 深入解答」。

当前版本使用同义词扩展、中文字符 n-gram、MySQL 中文全文索引、神经语义向量和关键词混合排序。开发环境未配置 Qdrant 时，从最多 1,500 条兜底候选资料选取 5 条；生产环境配置校内 Qdrant 后，先通过向量近邻索引取最多 80 条候选，再与关键词结果混合排序，不再随知识总量逐条扫描。本地返回前 3 条资料的相关摘录；用户主动深入解答时最多发送 5 段、每段 800 字符的资料，输出上限为 1000 Token，不自动重试。

本机已经安装 SentenceTransformers 和 CPU 版 PyTorch，中文模型位于 `backend/models/bge-small-zh-v1.5`，并通过 `LOCAL_EMBEDDING_MODEL` 启用。每条向量为 512 维，保存到数据库的 `knowledge_entries` 表中，因此查询时不会反复计算全部资料。模型无法加载时自动使用无模型依赖的本地字符向量，不影响知识库基本查询。

## 语义索引自动更新

知识内容上传后保持待审核状态，不进入公开检索。管理员审核通过时，系统在同一个审核事务中自动计算并保存本地向量，提交成功后立即可以检索；驳回时自动清除该内容的向量。检索时还会检查正文摘要、向量模型和内容指纹，发现内容变化、旧索引缺失或模型版本变化会自动重建。整个建索引流程不调用 DeepSeek，也不消耗外部 Token。

管理员可以通过 `GET /api/knowledge/embeddings/status` 查看已审核、已建索引和待更新数量，通过 `POST /api/knowledge/embeddings/rebuild` 手动补建。服务器维护时也可以在后端目录运行：

```powershell
python rebuild_knowledge_embeddings.py
python rebuild_knowledge_embeddings.py --force
```

第一条只补建缺失或过期向量，第二条强制重建全部已审核内容。通常不需要人工执行，仅用于更换模型后的批量预热或故障修复。

知识库页面的助手已增加权限智能体。学生可以说“查看我的课程”“查看批改进度”；教师可以说“查看我的课程”“查看批改进度”“生成某课程作业”“提醒未提交学生”。查询类操作直接执行；发布作业和发送提醒先返回预览，用户点击确认后才写入作业或消息。教师只能操作自己负责的课程，学生只能查看自己加入的课程和自己的提交，智能体不会越权访问其他课程。

深入解答沿用后端的 `DEEPSEEK_API_KEY` 配置。附件不自动解析，来源链接也不会自动抓取；请把需要 AI 查阅的信息写入正文或摘要。线上课程以资料和链接方式收录。

知识库查询、权限智能体和 DeepSeek 已分别设置每账号每分钟限额，默认分别为 60、60 和 10 次。配置 Redis 后，多台后端共享限额与检索缓存；未配置时自动使用单进程内存实现。管理员审核新资料或重建索引后，检索缓存版本立即更新。

管理员审核详情会提示正文中检测到的手机号、身份证号、邮箱和银行卡号。向 DeepSeek 发送问题、命中片段、教师上下文或对话历史前，系统会在本地替换这些标识；本地检索保持原文。该规则只能覆盖常见格式，正式部署还应增加学院/课程/班级范围权限、操作审计、附件病毒扫描和数据保留策略。

原教师教学助手保持原有功能，知识库助手位于独立的知识库页面。

## 启动与数据库

开发环境在后端启动时自动创建 `knowledge_entries` 表。已运行的后端需要重启加载新接口，前端开发服务自动更新；静态部署需使用最新构建产物。

生产环境关闭自动建表时，使用新增迁移 `a92c61e803bd`，在后端目录运行 `python -m flask --app run db upgrade`。该迁移兼容开发环境已创建新表的情况，不修改原有业务表。

验证：前端构建；`python -m unittest discover -s tests -v`，覆盖库权限、审核、附件鉴权、AI 资料筛选与服务失败回退。AI 测试模拟外部服务，不代表已验证线上模型连接。
