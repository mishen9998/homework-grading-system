# 模拟使用数据

已在现有 MySQL 数据库中追加一套可重复生成的演示数据，原有记录保留不变。演示记录使用 `sim26_` 前缀，方便管理员筛选。

## 数据规模

- 6 名模拟教师，每位教师创建 2 门课程，共 12 门课程。
- 300 名模拟学生，每 50 名学生组成一个班组；每门模拟课程固定 50 名学生。
- 24 份作业、192 道题目、1,020 份学生提交，其中 780 份已批改并含分数、逐题反馈和总评。
- 24 份课程资料、60 条学习笔记、600 条作业提醒、36 条师生聊天记录、12 条课表记录。

## 演示账号

所有 `sim26_` 演示账号密码均为 `Demo@2026!`：

- 教师：`sim26_teacher_01` 至 `sim26_teacher_06`
- 学生：`sim26_student_001` 至 `sim26_student_300`

原有管理员账号不会由演示脚本修改，其用户名和密码仅保存在本地环境，不纳入公开仓库。生产部署必须单独创建管理员并使用强随机密码。

详细账号列表、生成时间和本次新增数量保存在 [demo_seed_manifest.json](backend/demo_seed_manifest.json)。

## 重新生成

在后端目录运行：

```powershell
python seed_demo_data.py
```

脚本只创建缺失的 `sim26_` 记录，会保留原有数据，也不会重复加入课程、作业、提交、消息或聊天记录。若想扩充规模，可以修改脚本顶部的数量常量后运行；请使用新的用户名或先在管理员后台清理对应演示前缀的数据。

## 已验证场景

已启动后端 `http://127.0.0.1:5000` 和前端 `http://127.0.0.1:3000`，并验证教师登录、教师课程列表、课程学生列表、作业列表、提交列表和管理员统计接口。课程列表接口的日志输出也已改为应用日志，避免在后台进程运行时因中文控制台编码造成 500 错误。
